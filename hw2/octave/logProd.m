% The logProd function takes a vector of numbers in logspace 
% (i.e., x(i) = log p(i)) and returns the product of those numbers in logspace
% (i.e., logProd(x) = log(prod(p))
function [log_product] = logProd(x)
    %% Inputs %% 
    % x - 1D vector
    
    %% Outputs %%
    % log_product - scalar
    log_product = 0;
end
